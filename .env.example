# =============================================================================
# Environment Configuration
# =============================================================================
# This file contains example environment variables for the application.
# Copy this file to .env and fill in your actual values.
#
# IMPORTANT: Never commit the .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Admin Authentication (Required for /admin access)
# -----------------------------------------------------------------------------
# These values must be hex-encoded strings for security.
# Use the helper script to generate these values:
#
#   yarn generate:admin-secret
#
# The script will prompt you for a password and generate properly formatted
# hex-encoded values for both ADMIN_SECRET_SALT and ADMIN_SECRET_HASH.
#
# Format requirements:
# - ADMIN_SECRET_SALT: 64 character hex string (32 bytes)
# - ADMIN_SECRET_HASH: 128 character hex string (64 bytes)
# - Both must contain only characters: 0-9, a-f, A-F
#
# If either value is missing or invalid, admin authentication will be disabled.
ADMIN_SECRET_SALT=
ADMIN_SECRET_HASH=

# -----------------------------------------------------------------------------
# Supabase Database (Optional - enables database features)
# -----------------------------------------------------------------------------
# Get these from your Supabase project dashboard:
# https://app.supabase.com/project/_/settings/api
#
# SUPABASE_URL: Your project URL (format: https://xxx.supabase.co)
# SUPABASE_ANON_KEY: Public anon/public API key (safe for client-side use)
# SUPABASE_SERVICE_KEY: Service role key (server-side only, has elevated permissions)
#
# If not configured, the application will run without database features.
# All three values must be set for database functionality to work.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=

# -----------------------------------------------------------------------------
# Strava Integration (Optional - enables activity tracking)
# -----------------------------------------------------------------------------
# Required for Strava activity data integration.
# Get these from your Strava API application:
# https://www.strava.com/settings/api
#
# STRAVA_CLIENT_ID: Your application's client ID
# STRAVA_CLIENT_SECRET: Your application's client secret
# STRAVA_REFRESH_TOKEN: OAuth refresh token (obtain via OAuth flow)
#
# If not configured, Strava features will be disabled.
# All three values must be set for Strava integration to work.
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
STRAVA_REFRESH_TOKEN=

# -----------------------------------------------------------------------------
# GitHub Integration (Optional - enables stats page)
# -----------------------------------------------------------------------------
# Required for GitHub contribution graph on /stats page.
# Create a personal access token at:
# https://github.com/settings/tokens
#
# GH_PERSONAL_TOKEN: Personal access token with 'read:user' scope
# GITHUB_USERNAME: Your GitHub username
#
# If not configured, the stats page will display a friendly message.
# Both values must be set for GitHub stats to work.
GH_PERSONAL_TOKEN=
GITHUB_USERNAME=

# -----------------------------------------------------------------------------
# Sentry Error Tracking (Optional - enables error monitoring)
# -----------------------------------------------------------------------------
# Get these from your Sentry project settings:
# https://sentry.io/settings/
#
# SENTRY_DSN: Data Source Name (server-side)
# PUBLIC_SENTRY_DSN: Same DSN (client-side needs PUBLIC_ prefix)
# SENTRY_AUTH_TOKEN: Auth token for source map uploads
# SENTRY_ORG: Your Sentry organization slug
# SENTRY_PROJECT: Your Sentry project slug
#
# Free tier limits: 5k events/month, 30-day retention
# If not configured, error tracking will be disabled.
SENTRY_DSN=
PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=

# -----------------------------------------------------------------------------
# Development Notes
# -----------------------------------------------------------------------------
# Environment-specific configurations:
#
# Development:
# - Copy this file to .env for local development
# - Use yarn generate:admin-secret to create admin credentials
# - Supabase and Strava are optional during development
#
# Production:
# - Set environment variables through your hosting platform (Vercel, etc.)
# - Use different admin credentials than development
# - Ensure all required secrets are properly configured
# - Never expose SUPABASE_SERVICE_KEY or ADMIN_SECRET values publicly
# -----------------------------------------------------------------------------
